#include<iostream>
#include<string>
using namespace std;

int main(){
	string str;
	int num = 0;
	getline(cin, str);
	int len = str.length() - 1;
	while (len >= 0) {
		if (str[len] != ' ') {
			len--;
			num++;
			continue;
		}
		else break;
	}
	cout << num;
    return 0;
}
